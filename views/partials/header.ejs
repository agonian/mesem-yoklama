<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MESEM Yoklama Sistemi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; }
        .navbar-brand { font-weight: bold; letter-spacing: 1px; }
        .card { border: none; border-radius: 15px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .btn-custom { border-radius: 50px; padding: 10px 20px; }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 shadow">
  <div class="container">
    <a class="navbar-brand" href="/"><i class="fa-solid fa-school me-2"></i>MESEM YOKLAMA</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link" href="/"><i class="fa-solid fa-users me-1"></i>Öğrenciler</a></li>
        <li class="nav-item"><a class="nav-link" href="/odemeler"><i class="fa-solid fa-wallet me-1"></i>Ödemeler</a></li>
        <% if (ogrenciler.length > 0) { %>
           <li class="nav-item"><a class="nav-link" href="/yoklama-al"><i class="fa-solid fa-qrcode me-1"></i>Yoklama Al</a></li>
        <% } else { %>
            <li class="nav-item"><a class="nav-link" style="pointer-events: none;" href="/yoklama-al"><i class="fa-solid fa-qrcode me-1"></i>Yoklama Al</a></li>
        <% } %>
        
        <% if (ogrenciler.length > 0) { %>
           <li class="nav-item"><a class="nav-link" href="/raporlar"><i class="fa-solid fa-file-excel me-1"></i>Raporlar</a></li>
        <% } else { %>
            <li class="nav-item"><a class="nav-link" style="pointer-events: none;" href="/raporlar"><i class="fa-solid fa-file-excel me-1"></i>Raporlar</a></li>
        <% } %>     
        
        
      </ul>
    </div>
  </div>
</nav>

<div class="container">