<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MESEM Yoklama Sistemi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; }
        .navbar-brand { font-weight: bold; letter-spacing: 1px; }
        .card { border: none; border-radius: 15px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .btn-custom { border-radius: 50px; padding: 10px 20px; }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4 shadow">
  <div class="container">
    <a class="navbar-brand" href="/"><i class="fa-solid fa-school me-2"></i>MESEM YOKLAMA</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
  <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
    <li class="nav-item"><a class="nav-link" href="/"><i class="fa-solid fa-briefcase me-1"></i>İşletmeler / Anasayfa</a></li>
    <li class="nav-item"><a class="nav-link" href="/odemeler"><i class="fa-solid fa-wallet me-1"></i>Ödemeler</a></li>
    <li class="nav-item"><a class="nav-link" href="/raporlar"><i class="fa-solid fa-chart-pie me-1"></i>Raporlar</a></li>
    
    <% if (typeof aktifKullanici !== 'undefined' && aktifKullanici === 'yusuf.yilmz@gmail.com') { %>
        <li class="nav-item ms-lg-2">
            <a class="nav-link text-warning fw-bold border border-warning rounded px-2" href="/admin">
                <i class="fa-solid fa-crown me-1"></i>Admin Paneli
            </a>
        </li>
    <% } %>

    <% if (typeof aktifKullanici !== 'undefined') { %>
        <li class="nav-item ms-lg-4 d-flex align-items-center mt-2 mt-lg-0 border-start-lg ps-lg-3">
            <span class="navbar-text fw-bold text-white me-3 lh-1">
                <i class="fa-solid fa-user-circle me-1"></i> <%= aktifKullanici %>
            </span>
            <a class="btn btn-sm btn-light text-danger fw-bold border-0 shadow-sm" href="/logout">
                <i class="fa-solid fa-power-off me-1"></i>Çıkış Yap
            </a>
        </li>
    <% } %>
</ul>
    </div>
  </div>
</nav>

<div class="container">